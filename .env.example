# Optional HTTP server settings
PORT=8080

# Local stdio development only. The hosted HTTP deployment should not use
# a shared server-wide YouTube API key.
YOUTUBE_API_KEY=
YOUTUBE_TRANSCRIPT_LANG=

# Hosted HTTP deployment only. Set both values to enable usr_... key
# resolution via the MCP Key Service.
KEY_SERVICE_URL=
KEY_SERVICE_TOKEN=
